generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url      = env("DATABASE_URL")
}

model Profiles {
  id       String @id @default(auto()) @map("_id") @db.ObjectId
  Created  String
  Password String
  Username String
}
